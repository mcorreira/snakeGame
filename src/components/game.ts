import * as React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

export default function game(state): JSX.Element {
  return <SafeAreaView>{/* Game component */}</SafeAreaView>;
}
